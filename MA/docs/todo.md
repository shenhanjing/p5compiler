# 待完成内容
## 生成C++
* 将所有全局变量提取出来，并自动生成解包和打包的函数，全部放在一个struct/class中
* 将每个MAU作为独立的对象，继承全局变量基类，每个对象内部维护私有原全局变量同名成员，通过解包和打包函数实现这些成员的输入与输出
* MAU类型内置所有MAU功能，通过ma_id选取 vs 每个MAU有自己的类型：MAU的划分
* 将每个对象的声明与使用与接口函数对接上
* 提取所有table的总量，ID，匹配类型，键值类型，返回类型，自动生成search engine初始化代码以及key初始化代码
* 

## 仿真器功能
* 结构体（包含union）类型的key值构建，获取，与查表使用
* exact，lpm以及TCAM匹配类型的查表功能实现
* key值构建中的switch case与control parameters的规范转换逻辑
* _control_info函数的实现
* P5Table中switch case的规范转换逻辑


* 结构体键值获取时自动切片赋值给结构体，获取结构体键值的结构体成员信息，自动生成展开宏
* 对所有结构体添加拼接变量成员的函数，使得可以递归计算一个类型的位数。

# 已完成
* _compressed_*类型的声明转换
* table声明语句

* P5Table转换逻辑
* _apply函数调用
* 调用_key的赋值
* 调用_lookup的赋值